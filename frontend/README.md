# CoalMind console

React + Vite interface for the CoalMind API.

- Dev: `npm run dev` (proxies `/api` and `/health` to `http://127.0.0.1:8000`)
- Build: `npm run build`
- Hosted UI: set `VITE_API_URL` to the live API origin
